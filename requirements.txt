# Core
numpy>=1.24
torch>=2.1
gymnasium>=0.29
hydra-core>=1.3
omegaconf>=2.3
tqdm>=4.66
tensorboard>=2.14
rich>=13.7

# Graph / math helpers
networkx>=3.2

# SUMO (optional): if SUMO isn't installed system-wide, these may not work.
# Many SUMO installs ship `traci` and `sumolib` within $SUMO_HOME/tools.
traci>=1.20.0; platform_system != "Windows"
sumolib>=1.20.0; platform_system != "Windows"
